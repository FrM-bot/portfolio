---
// import { presentation } from "@/data/presentation"
import Link from "@/components/ui/Link.astro"
import SocialLinks from "./SocialLinks.astro"
import { getI18n } from "@/i18n"
import { convertAsteriskToStrongTag } from "@/utils"

const { currentLocale } = Astro

const i18n = getI18n({ lang: currentLocale })

const contact = i18n.Contact
---

<section class='flex flex-col gap-6'>
  <div class='flex w-full flex-row justify-between gap-2'>
    <h3
      class='text-lg text-neutral-100 underline decoration-dashed underline-offset-8'
    >
      {contact.title}
    </h3>
  </div>
  <p
  set:html={convertAsteriskToStrongTag(contact.message)}

  />
    <SocialLinks />
</section>
